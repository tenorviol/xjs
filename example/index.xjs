{{
  var dir = local.dir ? dir : __dirname;
}}
<html>
<body>
  <ul>
  {{
    var async = response.async();
    var files = require('fs').readdir(dir, function(err, files) {
      async.write(<li>{{=local}}</li>, files);
      async.end();
    });
  }}
  </ul>
</body>
</html>
